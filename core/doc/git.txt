# Get all remote branches

git fetch
git branch -v -a

# Put tags
# git pull --rebase --autostash

git remote set-url origin git@github.com:linuxconsole-org/ydfs.git
git tag -a v2.11.3 -m "Version 2.11.3"
git push origin v2.11.3
